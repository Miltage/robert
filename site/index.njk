---
layout: default
meta_title: Home
meta_description: Personal site of Robert Shenton
body_class: home
title: Hey friend!
subtitle: Below you'll find a collection of my efforts. The scribblings of a genius or ramblings of a raving lunatic? You decide.
---

<main id="main" class="main inner flex flex-1 flex-col md:flex-row justify-around py-10 lg:py-20 focus:outline-none" tabindex="-1">

    {% include "components/sidebar.njk" %}

    <section id="container-centre" class="column centre flex-1">

        <h1 class="page-title">{{ title }}</h1>

        <div class="content">

            <p>{{ subtitle }}</p>

            <h2 class="text-3xl">Things I've worked on</h2>

            <div class="items">

                {% for item in collections.project %}

                    {% if not item.data.hidden %}

                    <a href="{{ item.url }}"><item data-tippy-content="{{ item.data.title }}" style="background-image: url('{{ item.data.icon }}')"></item></a>

                    {% endif %}

                {% endfor %}

            </div>

            <div class="separator"></div>

            <h2 class="text-3xl">Musings</h2>

            <ol class="index">

                {% for post in collections.blog %}

                <li class="mb-1"><a href="{{ post.url }}">{{ post.data.title }}</a></li>

                {% endfor %}

            </ol>

        </div>

    </section>

</main>


